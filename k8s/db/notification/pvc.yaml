apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-notification-pvc
  namespace: services-db
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
